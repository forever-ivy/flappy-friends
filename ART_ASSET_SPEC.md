# 美术资产规范（ART_ASSET_SPEC）

本文约定 Skyline Hop 的全部游戏资产。目标：**替换素材只需要覆盖同名文件（或改资产清单），不需要改动玩法代码。**

- 资产清单（唯一被代码引用的入口）：`src/game/assets.ts`
- 当前占位素材目录：`public/assets/game/`
- 逻辑画布 **360×640**（Phaser `Scale.FIT` 缩放，9:16）。下表所有尺寸均为逻辑像素；用位图交付时请提供 **2× 分辨率**（SVG 则保持 viewBox 与下表一致，代码按逻辑尺寸栅格化加载）。

## 全局调色板

| 用途 | 色值 |
| --- | --- |
| 描边 / 深色底 | `#142436` |
| 纸白（高光 / 文字） | `#f7f1df` |
| 金（奖励 / 强调） | `#ffc857` |
| 珊瑚红（Nova / 危险） | `#ff5a73` |
| 薄荷绿（Moss） | `#36c6a1` |
| 紫罗兰（Violet） | `#8b78ff` |
| 天空基底 | `#b9ddd8` |

描边宽度：主体 4px、细节 3px（2× 资产翻倍）。所有游戏对象使用统一深色描边，保证复古像素城市风格的一致性。

## 角色（4 个）

文件：`character-{id}.svg`，`id ∈ nova | moss | sol | violet`。菜单头像复用同一文件，因此**必须透明背景**。

| 项 | 规格 |
| --- | --- |
| 画布 | 72×72（2× 位图：144×144） |
| 透明边距 | 四周 ≥ 6px（碰撞圆之外留白，避免视觉贴边） |
| 锚点 | 文件中心（36, 36）；Phaser 默认 0.5 origin，物理圆心即贴图中心 |
| 碰撞盒 | 圆形，半径 14（由清单 `collisionRadius` 定义，改半径只动清单） |
| 朝向 | 头朝右飞行姿态 |
| 失败表现 | 当前用 tint 变红实现（代码内置）；若提供失败帧请用 72×72 单帧命名 `character-{id}-fail.svg`，加进清单后由代码接入 |

**动画帧排列（未来升级路径）**：如需飞行动画，制作横向序列帧 `character-{id}-fly.png`，帧尺寸 72×72（2×：144×144），4–6 帧从左到右为一个完整扑翼循环，首帧保持与静态帧一致。届时只需在清单中登记帧数，加载与动画创建代码已预留 `textureKey` 命名规则。

## 背景（三层视差）

| 文件 | 逻辑尺寸 | 用法 |
| --- | --- | --- |
| `background-sky.svg` | 360×640 | 静态底层（天空、远山、云），不滚动 |
| `background-city.svg` | 720×640 | TileSprite，以 0.18× 障碍速度横向滚动（中景楼群） |
| `background-street.svg` | 720×180 | TileSprite，置于 y=565（画面底部 165px 高），全速滚动（街面、栏杆、路灯） |

**循环边缘（硬性要求）**：city 与 street 是无缝平铺，最左列与最右列像素必须连续（水平接缝不可见）。街面素材自上而下的结构：路缘 24px → 深色分隔 9px → 建筑立面/橱窗 111px → 路面 36px，替换时保持这四段的顺序与高度，否则会出现"楼飘在马路上"。

安全区：画面顶部 0–54px 与底部 574–640px 属于 UI/街面区，**中景楼群不要放置关键识别元素**，会被 HUD 或结算面板遮挡。

## 障碍

文件：`obstacle.svg`。

| 项 | 规格 |
| --- | --- |
| 画布 | 76×480（2×：152×960） |
| 物理体 | 58×470，偏移 (9, 5)——左右各留 9px、上留 5px 为描边/装饰缓冲（代码内置，随文件固定） |
| 用法 | 同一张图垂直翻转复用：上半根从顶垂下，下半根从底立起，成对出现 |
| 水平间距 | 一对障碍与下一对中心距 240（代码内置，含难度调参时不改图） |
| 间隙 | 190 / 175 / 165 / 155（按分数段，代码内置） |

注意：素材顶部的"管口/警示环"装饰必须在**文件顶端**，因为下半根会被翻转，装饰朝向自动正确。76px 宽度中仅中间 58px 参与碰撞，两侧各 9px 可自由设计（灯带、水渍等）。

## 奖励物

文件：`reward.svg`。

| 项 | 规格 |
| --- | --- |
| 画布 | 48×48（2×：96×96），透明背景 |
| 锚点 | 中心；生成于间隙中心 ±42px 内（代码计算安全偏移） |
| 动态 | 代码内以 2.4s/圈自转 + 拾取时 8 向粒子闪光——素材画正即可，不要自带旋转 |
| 视觉 | 五角星造型，金底珊瑚芯；与角色同风格深色描边 |

## 音效

当前为 WebAudio 合成（无文件），参数在清单 `SFX_CUES`（`src/game/assets.ts`）：`flap` 上滑短音、`score` 高频哔、`reward` 双音上行、`hit` 下坠噪声。替换为真实音频时只需改 `src/game/sfx.ts` 的实现，游戏代码与清单键名不变。

## 替换流程

1. 新文件与旧文件**同名同逻辑尺寸**，直接覆盖 `public/assets/game/` 下对应文件；或
2. 新文件名不同时，改 `src/game/assets.ts` 清单里的路径与尺寸参数（新角色需同步登记 `id/name/tagline/color/collisionRadius`）。
3. `npm run dev` 本地确认，替换后跑 `npm test`（碰撞盒与清单一致性由清单单测覆盖）。

任何情况下都不需要改 `src/game/scenes/Game.ts` 的玩法逻辑。
